import { styled, } from "src/stitches.config";

export const Button = styled("button", {
    backgroundColor: "$primary",
    border: "none",
    borderRadius: "5px",
    padding: "10px",
    color: "white",
    fontWeight: "bold",
    fontSize: "14px",
});